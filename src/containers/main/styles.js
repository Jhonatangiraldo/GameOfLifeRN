import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  title: {
    flex: 1,
  },
  grids: {
    flex: 10,
    margin: 10,
  }
});
export default styles;
