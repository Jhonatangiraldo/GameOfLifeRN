import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  wrapperTitle: {
    flexDirection: 'row',
    justifyContent: 'center',
    marginTop: 10,
  },
  title: {
    fontSize: 16,
    color: '#483d8b',
  },
});
export default styles;
